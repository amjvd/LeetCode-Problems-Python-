class Solution(object):
    def findNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        output = 0
        for x in nums:
            if len(str(x)) % 2 == 0:
                output +=1
        return output
