class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        num = 0
        count = 0
        for x in nums:
            if count == 0:
                num = x
                count +=1
            else:
                if x == num:
                    count +=1
                else:
                    count -=1
        return num
