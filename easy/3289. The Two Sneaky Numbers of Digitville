class Solution(object):
    def getSneakyNumbers(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        seen = {}
        dupe = []

        for x in nums:
            if x in seen:
                dupe.append(x)
                if len(dupe) == 2:
                    return dupe
            else:
                seen[x] = 1
